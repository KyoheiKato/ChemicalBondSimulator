# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

use_frameworks!

target 'ChemicalBondSimulator', exclusive: true do
	pod 'Realm'
end

target 'ChemicalBondSimulatorTests', exclusive: true do
  pod 'Realm/Headers'
end
